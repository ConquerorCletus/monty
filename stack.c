#include "monty.h"
/**
 * elem_stack - prints the top
 * @head: stack head
 * @lineCount: line count
 * Return: nothing
*/
void elem_stack(stack_t **head, unsigned int lineCount)
{
	(void)head;
	(void)lineCount;
	bus.lifi = 0;
}
